const config = {
  // domain: process.env ? "http://localhost:3000" : process.env.REACT_APP_DOMAIN,
  // api: process.env ? "http://localhost:8000" : process.env.REACT_APP_API,
  // api: "https://test.api.trackify.ai",
  // api: "https://new.api.trackify.ai",
  // api: "https://api.beta.trackify.ai",
  api: "http://localhost:8000",
};

export { config };
